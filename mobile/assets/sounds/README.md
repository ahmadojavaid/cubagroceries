# Sound Files

Place notification sound files here:

- `rider_alert.wav` — Plays when a delivery job is assigned to the rider (in-app alert via MethodChannel + notification channel sound).

This is a binary file that must be manually copied. It cannot be created via MCP tools.
